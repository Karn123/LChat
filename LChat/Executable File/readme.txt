  LChatServer -----服务器可执行文件
  LChatClient -----客户端可执行文件
  运行方法：由于服务端是控制台应用程序，所以要想看到服务端接受消息情况，可以在cmd中运行jar文件:打开cmd后,输入java -jar+空格，再把LChatServer.jar拖进去即可。显示“服务器端已启动”后，再双击运行LChatClient.jar即可
  
   悄悄话发送格式:/+用户昵称+空格+消息
   震屏命令:/shake